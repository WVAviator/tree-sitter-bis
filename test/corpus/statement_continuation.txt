==============
Line Separator
==============

@    LDV,P <run>,<title> \ Pack
     LDV,CU <rundesc>s40=<title> . Center, Uppercase

---

(source_file
  (statement
    (stmt_prefix)

    (ldv
      (call)
      (option)
      (named_variable)
      (named_variable)
    )

    (separator (comment))

    (ldv
      (call)
      (option)
      (option)
      (named_variable)
      (type)
      (named_variable)
    )

    (stmt_terminator)
    (comment)
  )
)
