==============
Line Separator
==============

@    LDV,P <run>,<title> \ Pack
     LDV,CU <rundesc>s40=<title> . Center, Uppercase

---

(source_file
  (statement
    (stmt_prefix)

    (call)
    (stmt_group
      (character)
    )
    (stmt_group
      (named_variable)
      (named_variable)
    )

    (separator (comment))

    (call)
    (stmt_group
      (character)
      (character)
    )
    (stmt_group
      (named_variable)
      (type)
      (operator)
      (named_variable)
    )

    (stmt_terminator)
    (comment)
  )
)
